# proxy_checker.py - Продвинутый чекер и оценщик прокси-конфигураций

[![Python](https://img.shields.io/badge/Python-3.7+-blue.svg?logo=python&logoColor=white)](https://www.python.org)
[![aiohttp](https://img.shields.io/badge/aiohttp-3.0+-green.svg)](https://docs.aiohttp.org/en/stable/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

**`proxy_checker.py`** - это мощный и гибкий Python-скрипт, предназначенный для автоматической проверки и оценки качества прокси-конфигураций, полученных из различных онлайн-источников. Скрипт поддерживает широкий спектр современных протоколов проксирования, предоставляет настраиваемую систему скоринга и ведет подробное логирование для отслеживания процесса работы.

## 🚀 Основные возможности

* **Поддержка множества протоколов:**
    * VLESS (`vless://`)
    * Shadowsocks (`ss://`, `ssconf://`)
    * Trojan (`trojan://`)
    * TUIC (`tuic://`)
    * HY2 (`hy2://`)
* **Продвинутый парсинг конфигураций:**  Разбирает URL-конфигурации и `ssconf://` строки, извлекая все ключевые параметры и обеспечивая поддержку различных опций.
* **Интеллектуальная система скоринга:**
    * Оценивает каждую прокси-конфигурацию на основе настраиваемых весов, учитывая протокол, методы шифрования, транспорт, параметры безопасности и другие характеристики.
    * Веса скоринга могут быть настроены пользователем через JSON-файл (`configs/scoring_weights.json`).
    * Включает механизм калибровки весов на основе линейной регрессии (в зачаточном состоянии).
* **Улучшенное логирование:**
    * Подробное логирование всех этапов работы скрипта в файл (`proxy_checker.log`) и консоль.
    * Цветной вывод в консоль для лучшей читаемости и выделения уровней важности сообщений (INFO, WARNING, ERROR, CRITICAL).
    * Различные форматы логирования для файла (полный формат с датой, временем, уровнем, процессом) и консоли (упрощенный формат).
* **Управление каналами:**
    * Загрузка URL-адресов каналов из файла `all_urls.txt`.
    * Проверка доступности каналов и повторные попытки загрузки при временных сбоях.
    * Ведение статистики по каждому каналу (количество валидных конфигураций, среднее время ответа, рейтинг стабильности).
    * Исключение нерабочих каналов из дальнейшей обработки и возможность их удаления из файла `all_urls.txt`.
* **Контроль уникальности прокси:**  Обеспечивает сохранение только уникальных прокси-конфигураций в выходном файле `proxy_configs.txt`, предотвращая дублирование на основе IP-адреса и порта.
* **Асинхронность и параллелизм:**
    * Использует `asyncio` и `aiohttp` для эффективной асинхронной загрузки и обработки данных, обеспечивая высокую производительность.
    * Ограничение на количество одновременных каналов и прокси для контроля нагрузки и предотвращения ошибок.
* **Настраиваемые параметры:**
    * `REQUEST_TIMEOUT`: Время ожидания ответа от канала (увеличено до 120 секунд).
    * `MAX_RETRIES`: Количество повторных попыток загрузки данных из канала при ошибках.
    * `RETRY_DELAY_BASE`: Базовый интервал задержки между повторными попытками.
    * `MIN_ACCEPTABLE_SCORE`: Минимальный приемлемый скор для прокси-конфигурации (по умолчанию 40.0).
    * `OUTPUT_CONFIG_FILE`: Путь к файлу для сохранения финальных конфигураций прокси (`configs/proxy_configs.txt`).
    * `ALL_URLS_FILE`: Путь к файлу со списком URL-адресов каналов (`all_urls.txt`).
    * `DEFAULT_SCORING_WEIGHTS_FILE`: Путь к файлу с весами скоринга (`configs/scoring_weights.json`).
* **Обработка ошибок и исключений:**  Реализована надежная система обработки исключений для различных ситуаций, таких как неверный формат URL, неподдерживаемые протоколы, ошибки парсинга и сетевые проблемы.
* **Сохранение статистики:**  В конце работы скрипт выводит сводную статистику о количестве обработанных каналов, найденных валидных конфигурациях, успешных и неудачных загрузках, а также разбивку по протоколам.

## ⚙️ Как использовать

1. **Предварительные требования:**
    * Python 3.7 или выше.
    * Установленные Python-библиотеки (указаны в разделе "Зависимости"). Вы можете установить их с помощью `pip`:
      ```bash
      pip install -r requirements.txt
      ```
      *(Файл `requirements.txt` необходимо создать самостоятельно и включить в него зависимости, смотрите раздел "Зависимости")*

2. **Настройка каналов:**
    * Создайте файл `all_urls.txt` в корневой директории скрипта или убедитесь, что он существует.
    * Добавьте в `all_urls.txt` URL-адреса источников (каналов), содержащих прокси-конфигурации, каждый URL на новой строке.
    * Убедитесь, что каждый URL канала является валидным и доступным.

3. **Настройка весов скоринга (опционально):**
    * Файл `configs/scoring_weights.json` содержит веса, используемые для оценки прокси-конфигураций.
    * Вы можете отредактировать этот файл, чтобы настроить систему скоринга под свои предпочтения, изменяя значения весов для различных параметров протоколов и общих характеристик.
    * Если файл `configs/scoring_weights.json` отсутствует, скрипт автоматически создаст его с весами по умолчанию.

4. **Запуск скрипта:**
    * Откройте терминал и перейдите в директорию, где расположен файл `proxy_checker.py`.
    * Запустите скрипт командой:
      ```bash
      python proxy_checker.py
      ```

5. **Результаты:**
    * После завершения работы скрипта, валидные и отсортированные по качеству прокси-конфигурации будут сохранены в файле `configs/proxy_configs.txt`.
    * Подробная информация о процессе работы скрипта, включая ошибки и предупреждения, будет записана в файл `proxy_checker.log` и отображена в консоли.
    * В консоли также будет выведена сводная статистика о результатах проверки.

## 🗂️ Структура проекта


proxy_checker.py # Основной скрипт чекера прокси
configs/
scoring_weights.json # Файл с весами для скоринга прокси
proxy_configs.txt # Файл для сохранения финальных конфигураций прокси (создается при запуске)
all_urls.txt # Файл со списком URL-адресов каналов (необходимо создать)
proxy_checker.log # Файл логов (создается при запуске)

## ⚙️ Конфигурация `scoring_weights.json`

Файл `configs/scoring_weights.json` позволяет настраивать систему оценки прокси, изменяя веса различных параметров.  Это JSON-файл, содержащий словарь, где ключи - это имена параметров скоринга (в виде строк), а значения - числовые веса (целые или дробные числа).

**Пример содержимого `scoring_weights.json`:**

```
{
    "PROTOCOL_BASE": 20,
    "CONFIG_LENGTH": 5,
    "RESPONSE_TIME": -0.1,
    "AGE_PENALTY": -0.05,
    "CHANNEL_STABILITY": 15,

    "VLESS_SECURITY_TLS": 15,
    "VLESS_SECURITY_NONE": -10,
    "VLESS_TRANSPORT_WS": 10,
    "VLESS_TRANSPORT_TCP": 2,
    "VLESS_ENCRYPTION_NONE": -5,
    "VLESS_ENCRYPTION_AUTO": 5,
    "VLESS_ENCRYPTION_AES_128_GCM": 8,
    "VLESS_ENCRYPTION_CHACHA20_POLY1305": 8,
    "VLESS_UUID_PRESENT": 5,
    "VLESS_EARLY_DATA": 3,
    "VLESS_SNI_PRESENT": 7,
    "VLESS_ALPN_PRESENT": 5,
    "VLESS_PATH_PRESENT": 3,

    "SS_METHOD_CHACHA20_IETF_POLY1305": 15,
    "SS_METHOD_AES_256_GCM": 14,
    "SS_METHOD_AES_128_GCM": 12,
    "SS_METHOD_NONE": -20,
    "SS_PASSWORD_LENGTH": 5,
    "SS_PLUGIN_OBFS_TLS": 10,
    "SS_PLUGIN_OBFS_HTTP": 8,
    "SS_PLUGIN_NONE": 0,

    "SSCONF_SERVER_PORT": 5,
    "SSCONF_METHOD_CHACHA20_IETF_POLY1305": 15,
    "SSCONF_METHOD_AES_256_GCM": 14,
    "SSCONF_METHOD_AES_128_GCM": 12,
    "SSCONF_METHOD_NONE": -20,
    "SSCONF_PASSWORD_LENGTH": 5,
    "SSCONF_PROTOCOL_ORIGIN": 3,
    "SSCONF_PROTOCOL_AUTH_SHA1_V4": 7,
    "SSCONF_PROTOCOL_AUTH_AES128_CFB": 7,
    "SSCONF_OBFS_PLAIN": 0,
    "SSCONF_OBFS_TLS": 10,
    "SSCONF_OBFS_HTTP": 8,
    "SSCONF_OBFS_WEBSOCKET": 10,
    "SSCONF_UDP_OVER_TCP": 5,

    "TROJAN_SECURITY_TLS": 15,
    "TROJAN_TRANSPORT_WS": 10,
    "TROJAN_TRANSPORT_TCP": 2,
    "TROJAN_PASSWORD_LENGTH": 5,
    "TROJAN_SNI_PRESENT": 7,
    "TROJAN_ALPN_PRESENT": 5,
    "TROJAN_EARLY_DATA": 3,

    "TUIC_SECURITY_TLS": 15,
    "TUIC_TRANSPORT_WS": 10,
    "TUIC_TRANSPORT_UDP": 5,
    "TUIC_CONGESTION_CONTROL_BBR": 8,
    "TUIC_CONGESTION_CONTROL_CUBIC": 5,
    "TUIC_CONGESTION_CONTROL_NEW_RENO": 3,
    "TUIC_UUID_PRESENT": 5,
    "TUIC_PASSWORD_LENGTH": 5,
    "TUIC_SNI_PRESENT": 7,
    "TUIC_ALPN_PRESENT": 5,
    "TUIC_EARLY_DATA": 3,
    "TUIC_UDP_RELAY_MODE": 7,
    "TUIC_ZERO_RTT_HANDSHAKE": 6,

    "HY2_SECURITY_TLS": 15,
    "HY2_TRANSPORT_UDP": 5,
    "HY2_TRANSPORT_TCP": 2,
    "HY2_PASSWORD_LENGTH": 5,
    "HY2_SNI_PRESENT": 7,
    "HY2_ALPN_PRESENT": 5,
    "HY2_EARLY_DATA": 3,
    "HY2_PMTUD_ENABLED": 4,
    "HY2_HOP_INTERVAL": 2,

    "COMMON_PORT_443": 10,
    "COMMON_PORT_80": 5,
    "COMMON_PORT_OTHER": 2,
    "COMMON_UTLS_CHROME": 7,
    "COMMON_UTLS_FIREFOX": 6,
    "COMMON_UTLS_RANDOMIZED": 5,
    "COMMON_UTLS_OTHER": 2,
    "COMMON_CDN": 8,
    "COMMON_OBFS": 4,
    "COMMON_HEADERS": 3,
    "COMMON_RARE_PARAM": 4,
    "COMMON_HIDDEN_PARAM": 2
}
IGNORE_WHEN_COPYING_START
content_copy
download
Use code with caution.
IGNORE_WHEN_COPYING_END
```

Вы можете изменять значения весов, чтобы подчеркнуть важность определенных характеристик прокси. Например, увеличение веса VLESS_SECURITY_TLS повысит рейтинг VLESS-прокси с TLS-шифрованием.

📜 Логирование

Скрипт использует продвинутую систему логирования, которая обеспечивает подробное отслеживание процесса работы. Логи записываются в два места:

Файл proxy_checker.log: Содержит полную историю работы скрипта, включая сообщения всех уровней важности (DEBUG, INFO, WARNING, ERROR, CRITICAL) и дополнительную информацию, такую как время, уровень сообщения и ID процесса. Формат файла:

%(asctime)s [%(levelname)s] %(message)s (Process: %(process)s)
IGNORE_WHEN_COPYING_START
content_copy
download
Use code with caution.
IGNORE_WHEN_COPYING_END

В файл записываются сообщения уровня WARNING и выше (WARNING, ERROR, CRITICAL).

Консоль: Отображает сообщения уровня INFO и выше (INFO, WARNING, ERROR, CRITICAL) в упрощенном и цветном формате для удобства чтения. Цвет используется для визуального выделения уровня важности сообщения:

INFO: Зеленый цвет

WARNING: Желтый цвет

ERROR: Красный цвет

CRITICAL: Жирный красный цвет

Формат консольного вывода:

[%(levelname)s] %(message)s
IGNORE_WHEN_COPYING_START
content_copy
download
Use code with caution.
IGNORE_WHEN_COPYING_END

Такая система логирования позволяет как оперативно отслеживать работу скрипта в консоли, так и анализировать подробную историю работы, сохраненную в файле логов.

⚠️ Обработка ошибок

Скрипт разработан с учетом надежности и включает в себя обработку различных типов ошибок и исключений:

InvalidURLError: Вызывается при обнаружении URL-адреса неверного формата.

UnsupportedProtocolError: Возникает, если URL-адрес использует неподдерживаемый протокол.

InvalidParameterError: Используется при обнаружении неверных параметров в URL-адресе.

ConfigParseError: Генерируется при ошибках разбора параметров конфигурации (например, в ssconf://).

Сетевые ошибки aiohttp (ClientResponseError, TimeoutError, ClientError): Обрабатываются при загрузке данных из каналов, реализованы повторные попытки с экспоненциальной задержкой.

Непредвиденные исключения: Перехватываются общие исключения для предотвращения аварийного завершения скрипта и записи подробной информации об ошибке в лог.

В случае возникновения ошибок, скрипт старается продолжить работу, если это возможно, и информирует пользователя о проблемах через систему логирования.

📦 Зависимости

Скрипт зависит от следующих Python-библиотек, которые необходимо установить перед использованием:

asyncio: Встроенная библиотека для асинхронного программирования.

aiohttp: Асинхронный HTTP-клиент для загрузки данных из каналов.

aiodns: Асинхронный DNS-резолвер для разрешения доменных имен в IP-адреса.

re: Встроенная библиотека для работы с регулярными выражениями.

os: Встроенная библиотека для работы с операционной системой.

json: Встроенная библиотека для работы с JSON-данными.

logging: Встроенная библиотека для логирования.

ipaddress: Встроенная библиотека для работы с IP-адресами.

io: Встроенная библиотека для работы с потоками ввода-вывода.

uuid: Встроенная библиотека для работы с UUID.

numbers: Встроенная библиотека для числовых типов данных.

functools: Встроенная библиотека для функций высшего порядка и операций над вызываемыми объектами.

string: Встроенная библиотека для строковых констант и операций.

socket: Встроенная библиотека для низкоуровневого сетевого интерфейса.

base64: Встроенная библиотека для кодирования/декодирования Base64.

enum: Встроенная библиотека для перечислений (Enum).

urllib.parse: Встроенная библиотека для парсинга URL.

datetime: Встроенная библиотека для работы с датой и временем.

typing: Встроенная библиотека для аннотаций типов.

dataclasses: Встроенная библиотека для датаклассов.

collections: Встроенная библиотека для специализированных контейнерных типов данных.

numpy: Библиотека для научных вычислений, используется для линейной регрессии при калибровке весов.

sklearn (scikit-learn): Библиотека машинного обучения, используется для линейной регрессии при калибровке весов.

